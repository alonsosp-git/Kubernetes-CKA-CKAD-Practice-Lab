apiVersion: v1
kind: Pod
metadata:
  name: sized
  labels:
    app: sized
spec:
  containers:
    - name: app
      image: nginx:1.25
      resources:
        requests:
          cpu: 250m
          memory: 256Mi
        limits:
          cpu: 250m
          memory: 256Mi
